version: '3.8'
services:
  mongodb:
    image: mongo:6-jammy
    ports:
      - '27017:27017'
    volumes:
      - dbdata6:/data/db

  plman: 
    deploy:
      mode: global
    image: plman
    ports:
      - '80:80'

    depends_on:
      - mongodb
volumes:
  dbdata6: